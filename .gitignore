# Python 관련 파일들
*.py[cod]        # 컴파일된 Python 파일들
*.pyo
*.pyd

# 가상 환경 관련
venv/            # 가상환경 폴더 (venv)
env/             # 다른 이름의 가상환경 폴더
.Python          # 가상환경의 Python 바이너리 파일

# 패키지들
*.egg
*.egg-info/
dist/
build/
__pycache__/     # Python에서 생성하는 캐시 파일

# IDE 관련 파일들
.vscode/         # Visual Studio Code 설정 폴더
.idea/           # PyCharm 같은 JetBrains 계열 IDE 설정 폴더
*.sublime-workspace
*.sublime-project

# 운영체제 관련 파일들
.DS_Store        # macOS에서 생성하는 파일
Thumbs.db        # Windows에서 생성하는 파일

# 로그 파일들
*.log
*.out

# 특정 도구나 라이브러리 관련
.ipynb_checkpoints   # Jupyter Notebook 체크포인트 파일들
migrations/          # Django 마이그레이션 파일들 (프로젝트에 따라 관리가 필요할 수 있음)
